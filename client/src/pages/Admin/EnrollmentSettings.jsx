import PropTypes from "prop-types";

EnrollmentSettings.propTypes = {
  p: PropTypes.string,
};

export default function EnrollmentSettings({ p }) {
  return <div className="h-full w-full py-5"></div>;
}
